'use strict';

module.exports = function(Hospital) {
//   const login = Hospital.login;
//   Hospital.login = function(credentials, include, callback) {
//     if (typeof include === 'function') {
//       callback = include;
//       include = undefined;
//     }

//     login.call(this, credentials, include, function(err, accessToken) {
//       if (err) return callback(err);

//           // Get user details based on the userId in the access token
//       Hospital.findById(accessToken.userId, function(err, userDetails) {
//         if (err) return callback(err);

//             // Include the user details in the response
//         accessToken.userDetails = userDetails;
//         callback(null, accessToken);
//       });
//     });
//   };
};
